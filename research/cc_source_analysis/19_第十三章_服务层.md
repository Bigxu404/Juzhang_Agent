description: 分析底层的API网络请求客户端配置，以及对接外部的Model Context Protocol (MCP)和GrowthBook等特性控制门控功能。

## 第十三章：服务层 ​

### 13.1 API客户端 ​

bash

```
head -40 services/api/claude.ts
```

Claude API调用的底层封装。

### 13.2 MCP服务 ​

bash

```
ls services/mcp/
```

Model Context Protocol客户端实现，包括：

* `client.ts` — MCP客户端
* `server.ts` — 本地MCP服务器
* `officialRegistry.ts` — 官方注册表
* `vscodeSdkMcp.ts` — VS Code集成

### 13.3 分析服务 ​

bash

```
head -40 services/analytics/growthbook.ts
```

GrowthBook特性门控——远程控制feature flag的运行时开关。

---